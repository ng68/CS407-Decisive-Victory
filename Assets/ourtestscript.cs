using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ourtestscript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //whats good homies
    }

    // Update is called once per frame
    void Update()
    {
        //we out here writing scripts
    }
}
